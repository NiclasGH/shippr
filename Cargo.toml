[package]
name = "shippr"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = [ "derive" ] }
config = "0.15"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
tempfile = "3.15.0"
